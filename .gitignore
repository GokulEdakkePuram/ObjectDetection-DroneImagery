# Datasets and model artifacts are large and reproducible; never commit them.
data/
runs/
weights/
*.pt
*.onnx
*.mlpackage/
*.torchscript

# reports/ is regenerated by `make profile` / `aerialdet eval`; the numbers
# worth keeping are transcribed into docs/ alongside their interpretation.
reports/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# OS / editor
.DS_Store
.idea/
.vscode/
