# A one-epoch run used to prove the pipeline end-to-end before committing
# hours of compute to it.
extends: base
name: smoke
model: yolo11n.pt
imgsz: 640
batch: 4
epochs: 1
patience: 0
train_args:
  fraction: 0.02
  plots: false
